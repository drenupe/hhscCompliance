{
  "version": 2,
  "installCommand": "npm ci",
  "buildCommand": "npx nx build web --configuration=production && echo \"--- dist/web ---\" && ls -la dist/web && test -f dist/web/index.html",
  "outputDirectory": "dist/web",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
