{
  "name": "consumers",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "features/consumers/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:feature", "domain:consumer"],
  "targets": {
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "features/consumers/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
