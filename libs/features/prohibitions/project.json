{
  "name": "prohibitions",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/features/prohibitions/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["type:lib", "scope:feature", "feature:prohibitions", "surface:web"],
  "targets": {
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/features/prohibitions"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
